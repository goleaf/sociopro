# Known Technical Debt

Generated: 2026-07-02

This register tracks remaining risks that should not be silently normalized. Each item needs a focused follow-up with tests and rollback notes.

| Priority | Area | Risk | Reason not fixed now | Recommended next step |
| --- | --- | --- | --- | --- |
| P0 | `app/Http/Controllers/ApiController.php` | God controller with inline validation, mixed rendering/API concerns, and high regression risk. | Splitting the full API in one pass would change too many contracts. | Pick one route group, add API contract tests, extract Form Requests/actions/resources, then repeat. |
| P0 | State-changing GET routes in `routes/web.php` and `routes/custom_routes.php` | CSRF, crawler/prefetch side effects, and accidental destructive actions. | Changing verbs breaks existing Blade/JS callers without route-by-route coverage. | Convert one route family to POST/DELETE with CSRF, update callers, and add method/authorization tests. |
| P0 | Raw Blade output and rich-text rendering hotspots | Stored or reflected XSS if untrusted content reaches `{!! !!}` output. | Requires sanitizer policy and UI regression tests to avoid breaking existing formatting. | Add XSS payload tests for chat/posts/profile snippets, define sanitizer rules, then replace unsafe output. |
| P0 | Payment callbacks and payment state transitions | Signature, replay, and idempotency gaps can create incorrect payment state. | Provider-specific behavior needs focused fixtures and test-mode setup. | Inventory each gateway callback, add signed request/idempotency tests, then extract payment transition actions. |
| P0 | API route handler drift | Registered routes currently include handlers that are not callable: `api.pages.likes.destroy` targets `ApiController::page_dislike`, `api.groups.members.destroy` targets `ApiController::groups_join_remove`, and `api.jobs.wishlist.index` targets `ApiController::job_wishlist`. | This task locks route registration and authentication behavior only; adding handlers or renaming routes would change the legacy API surface. | Decide whether each route must remain public compatibility surface, then add endpoint-specific tests before restoring handlers or deprecating the routes. |
| P1 | Legacy install SQL baseline | Schema ownership is split between `database/schema/install.sql` and additive migrations. | A baseline migration needs production schema/data comparison before replacing the dump. | Produce schema diff, decide baseline migration strategy, and document rollback for dump removal. |
| P1 | Upload validation still inline in legacy controllers | Inconsistent validation and harder storage faking for images/videos. | Broad extraction would touch many user-facing flows. | Extract Form Requests for chat, profile, page, group, fundraiser, and admin upload surfaces one at a time. |
| P1 | Web chat invalid upload handling | Invalid executable extensions on `POST /chat/save` reach `FileUploader`, throw a 500, and leave a `message_thrades`/`chats` row without a `media_files` row. | This pass is behavior-preserving characterization before chat naming/refactor work; changing response and transaction behavior could affect legacy clients. | Move upload validation before thread/chat persistence, return the existing `validationError` shape, wrap message and media creation in a transaction, and keep `LegacyWebChatTest` coverage green. |
| P1 | `app/Http/Controllers/ChatController.php` and `app/Http/Controllers/ApiController.php` chat authorization architecture | Participant checks now protect covered web/API chat read, delete, and reaction paths, but the checks still live in legacy controllers instead of a dedicated policy/action layer. Web chat delete also remains a state-changing GET route. | Moving chat to policies/actions and changing route verbs can break legacy Blade/JS/API clients without a focused compatibility pass. | Extract chat participant authorization to a policy or action, migrate web delete to a CSRF-protected verb, update callers, and keep the current denial tests green. |
| P1 | `app/Http/Controllers/ChatController.php` search query shape | `search.chat` now renders escaped HTML through a Blade partial, but it still performs a per-contact last-message lookup inside the controller loop. | Reworking the query shape needs a query-count regression test and careful preservation of the legacy HTML-string response contract. | Move contact search data assembly into a scoped query/action, precompute latest messages without per-row queries, and keep the XSS coverage green. |
| P1 | Remaining page/API authorization surfaces | Web page edit, cover, info, and owner modals are policy-protected, but page API endpoints and secondary page actions still need endpoint-by-endpoint authorization review. | This pass intentionally kept the tested web page owner flow reviewable. | Add API contract tests for page create/update/delete/media actions, then enforce the same `PagePolicy` semantics and rate limits. |
| P1 | Legacy API auth payload exposure | `POST /api/login` returns a full legacy `user` payload alongside `user_id`, image fields, token, and token expiry. | Contract tests now document the existing response shape; changing it could break mobile clients. | Audit sensitive user fields, introduce an API Resource behind compatibility tests, and version or stage any response-field removal. |
| P1 | Legacy API validation gaps | `POST /api/create_post` currently accepts an empty payload and creates a post with empty content, and several write endpoints return inconsistent validation/error shapes. | This task is characterization only, so the tests preserve current behavior instead of tightening validation. | Add endpoint-specific validation tests and Form Requests before changing response behavior. |
| P1 | Public media storage contract | Private content metadata can still point to public files for legacy playback. | Moving files private would break existing URLs and requires migration/backfill. | Design a private media access layer, migrate one media type, and keep compatibility tests. |
| P1 | CSP compatibility allowances | Current CSP still permits `'unsafe-inline'`, `'unsafe-eval'`, broad `https:` sources, and live-video exceptions. | Legacy Blade inline handlers, widgets, payment SDKs, and Zoom live video need compatibility. | Move inline scripts/styles into compiled assets, replace broad sources with provider hostnames, and tighten in report-only mode first. |
| P1 | Full npm dependency audit | Full dev audit currently includes Laravel Mix/Webpack-era vulnerabilities, while runtime audit is expected to be lower risk. | Mix-to-Vite/build-tool migration is a larger asset pipeline change. | Keep `npm audit --omit=dev` in release review and plan a dedicated Mix-to-Vite migration. |
| P1 | Queue/scheduler production posture | `.env.example` defaults to sync queue and no scheduler tasks are committed. | Real worker/scheduler infrastructure is host-specific. | Document host supervisor/cron setup per environment before enabling async production jobs. |
| P2 | Admin architecture | Admin screens are legacy controller/views, not Filament resources. | Filament is not installed despite older stack notes. | Either document legacy admin as the supported path or plan a separate Filament installation/migration. |
| P2 | Test coverage distribution | High-risk flows now have many audit tests, but some modules still rely on broad smoke coverage. | Exhaustive coverage in one prompt would be too large to review safely. | Continue adding focused tests for imports/exports, notifications/mail, soft deletes, cache invalidation, and tenant/account isolation. |
| P2 | CORS/proxy/cookie production hardening | Defaults are safe for local development but need environment-specific allowlists and HTTPS/proxy settings. | Production host/proxy topology is not represented in this repo. | Set `CORS_ALLOWED_ORIGINS`, HTTPS cookie settings, and trusted proxy handling in deployment documentation for each environment. |

## Verification Notes

- Add a test or explicit documentation entry for every legacy behavior before refactoring it.
- Keep `composer audit --no-interaction` and `npm run quality` in CI.
- Treat any failed quality command as release-blocking unless this file records the exact command, failure, risk, and owner-approved next step.

## Known Command Failures

| Command | Summary | Risk | Next step |
| --- | --- | --- | --- |
| `php artisan test tests/Feature` | Pre-task baseline on 2026-07-04 was already red. The run showed `Tests\Feature\ApiPostAuthorizationTest` timestamp mismatches, order-sensitive auth/ownership failures, `Tests\Feature\ContentOwnershipAuthorizationTest::test_api_unfollow_only_removes_the_current_users_follow_row` failing with `Call to undefined method App\Models\Follower::follower()`, blog ownership API tests returning 200 instead of 403, and then aborted with a 128 MB memory fatal in `Tests\Feature\PageControllerTest::test_store_update_cover_and_info_routes_mutate_only_current_users_page` through `Intervention\Image\Drivers\Gd\Decoders\FilePathImageDecoder`. | Feature-suite verification can stop before the new API contract tests run, and unrelated red tests can hide API contract regressions. | Keep running `php artisan test tests/Feature/Api/Contracts` for API extraction work, then fix the pre-existing feature-suite failures in focused follow-up tasks before treating the full feature suite as release-gating again. |
| `php artisan test` | 2026-07-04 verification for the API contract task aborted under the default 128 MB PHP memory limit while rendering a `MethodNotAllowedHttpException` response in `Tests\Feature\InstallWizardTest::test_install_post_steps_reject_unexpected_http_methods`; the fatal was `Allowed memory size of 134217728 bytes exhausted` in `Illuminate\Foundation\Exceptions\Renderer\Renderer::js()`. | The full-suite process exits before completion even though the new API contract suite is green, so full-suite status cannot currently be used as proof of this task's safety. | Reproduce `InstallWizardTest` in isolation, avoid rendering the full exception page for method-contract assertions, and profile full-suite memory growth before relying on `php artisan test` as a required gate. |
| `php artisan test` | Fails under the default 128 MB PHP memory limit during the full suite at `Tests\Feature\MediaDownloadSecurityTest::test_private_media_download_requires_owner`; Symfony `FileinfoMimeTypeGuesser` exhausts memory while preparing a `BinaryFileResponse`. The focused command `php artisan test tests/Feature/MediaDownloadSecurityTest.php --filter=test_private_media_download_requires_owner` passes. A 2026-07-04 full run also displayed order-dependent failures for `Tests\Feature\ContentOwnershipAuthorizationTest::test_api_user_cannot_update_another_users_blog_or_take_ownership` and `::test_api_user_cannot_delete_another_users_blog` before the fatal; `php artisan test tests/Feature/ContentOwnershipAuthorizationTest.php` passes in isolation. | Full-suite verification can abort before later tests run, and order/state-sensitive failures can be hidden by the later memory fatal. | Profile full-suite memory growth, then either reduce leaked/static test state before `MediaDownloadSecurityTest` or document and enforce a CI PHP memory limit that can complete the suite. Add an order-dependence audit for the content ownership API tests if they reappear after the memory blocker is removed. |
| `php artisan test` | 2026-07-04 chat refactor verification aborted under the default 128 MB PHP memory limit at `Tests\Feature\MainControllerValidationTest::test_create_post_with_image_upload_creates_public_post_image`; `Intervention\Image\Gd\Decoder` exhausted memory while `FileUploader::putResizedImage()` processed a fake post image. | The full suite still cannot complete under the default memory limit, so chat-focused passing tests are the reliable safety signal for this scoped refactor. | Reproduce `MainControllerValidationTest` in isolation, make upload tests use smaller deterministic fixtures or a fake image adapter, then rerun the full suite before treating `php artisan test` as green. |
| `npm audit --audit-level=moderate` | Fails with 11 dev-tool vulnerabilities through the Laravel Mix/Webpack chain: `elliptic` via `browserify-sign`/`crypto-browserify`/`node-libs-browser`/`laravel-mix`, and `uuid` via `node-notifier`/`webpack-notifier`/`webpack-dev-server`. npm reports no direct fix available. | Development/build tooling exposure; `npm audit --omit=dev --audit-level=moderate` currently reports 0 runtime vulnerabilities. | Keep runtime audit in release review, restrict build tooling to CI/trusted hosts, and plan the dedicated Mix-to-Vite migration. |
