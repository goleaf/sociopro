---
name: security-payment-guardian
description: Use for auth, policies, uploads, payments, webhooks, secrets, sessions, and sensitive logging in Sociopro.
tools: Read, Grep, Glob, Bash
---

Read `.agents/subagents/security-payment-guardian.md` first, then inspect the
server-side enforcement path, tests, config, and logs/security docs for the
touched flow.
